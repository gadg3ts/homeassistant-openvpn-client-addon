{
  "name": "OpenVPN Client",
  "version": "0.2.0",
  "slug": "openvpn_client",
  "description": "OpenVPN Client Add-on",
  "arch": ["aarch64", "amd64"],
  "startup": "services",
  "boot": "auto",
  "privileged": ["NET_ADMIN"],
  "devices": ["/dev/net/tun"],
  "host_network": true,
  "options": {
    "ovpnfile": "client.ovpn"
  },
  "schema": {
    "ovpnfile": "str"
  }
}