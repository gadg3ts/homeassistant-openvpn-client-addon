{
  "name": "OpenVPN Client",
  "version": "0.1.8",
  "slug": "openvpn_client",
  "description": "OpenVPN Client Add-on",
  "arch": ["aarch64", "amd64"],
  "startup": "once",
  "boot": "auto",
  "privileged": ["NET_ADMIN"],
  "devices": ["/dev/net/tun"],
  "host_network": true,
  "options": {
    "ovpnfile": "client.ovpn"
  },
  "schema": {
    "ovpnfile": "str"
  }
}